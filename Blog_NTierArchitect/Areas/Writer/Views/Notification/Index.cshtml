@model IEnumerable<EntityLayer.Concrete.Notification>

@{
    ViewData["Title"] = "Bildirişlər";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}


<table class="table">
    <thead>
        <tr>
            <th>
                Tip
            </th>
            <th>
                Simvol
            </th>
            <th>
                Mətn
            </th>
            <th>
                Status
            </th>
            <th>
                Tarix
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr style="border: 4px solid @item.Color">
                <td>
                    @item.Type
                </td>
                <td>
                    <i class="@item.Symbol"></i>
                </td>
                <td>
                    @item.Details
                </td>
                <td>
                    @if (item.Status)
                    {
                        <span class="badge badge-success">Oxunmadı</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">Oxundu</span>
                    }
                </td>
                <td>
                    ~ @(Math.Round((DateTime.Now - item.NotificationDate).TotalDays, 0)) gün əvvəl
                </td>
                <td>
                    <big><a href="/writer/Notification/IsRead/@item.ID" title="Oxundu olaraq işarələ"><i class="mdi mdi-read border-size-xl"></i></a></big>
                </td>
            </tr>
        }
    </tbody>
</table>

